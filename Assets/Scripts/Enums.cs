using UnityEngine;

/// <summary>
/// Статический класс, для хранения Enum'ов
/// </summary>
public static class Enums
{
    /// <summary>
    /// Цвета для объекта
    /// </summary>
    public enum ObjectColor
    {
        Red,
        Green, 
        Blue,
        Orange,
        Yellow,
        LightBlue
    }    
}
